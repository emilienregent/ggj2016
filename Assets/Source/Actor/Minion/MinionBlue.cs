using UnityEngine;
using System.Collections;

public class MinionBlue : Minion {

	// Use this for initialization
	private void Awake () {
		this.SetColor(MinionColor.BLUE);
	}
	
	// Update is called once per frame
	private void Update () {
	
	}
}